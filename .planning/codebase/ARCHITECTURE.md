# ARCHITECTURE.md - Architecture

Mapping Complete

**Focus:** arch

This document was generated by the architecture-focused mapper agent.

## High-level Patterns

- CLI-first: `src/cli.ts` is the entrypoint. Commands are implemented under `src/create/` and `src/build/`.
- Feature-based organization rather than strict layers.

## Key Entry Points

- `src/cli.ts` — main executable
- `src/create/create.ts`, `src/build/build.ts` — primary flows

Documents written:

- `.planning/codebase/ARCHITECTURE.md` (this file)
- `.planning/codebase/STRUCTURE.md` (created by the same mapper)

Ready for orchestrator summary.
