# STACK.md - Technology Stack

Mapping Complete

**Focus:** tech

This document was generated by the tech-focused mapper agent.

## Languages & Runtimes

- TypeScript (primary language) — `src/`
- Node.js runtime for CLI tooling

## Build & Tooling

- `tsc` (TypeScript compiler) — `tsconfig.json` and `src/tsconfig.json`
- `pnpm`/`npm` used for dependency management; `package.json` contains scripts
- Vitest for tests (`vitest.config.ts`)

## Key Dependencies

- `typescript`, `prettier`, `eslint` (see `package.json`)

## Config Files

- `package.json`, `tsconfig.json`, `.github/workflows/`

Documents written:

- `.planning/codebase/STACK.md` (this file)
- `.planning/codebase/INTEGRATIONS.md` (created by the same mapper)

Ready for orchestrator summary.
